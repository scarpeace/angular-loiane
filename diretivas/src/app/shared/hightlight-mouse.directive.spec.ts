import { HightlightMouseDirective } from './hightlight-mouse.directive';

describe('HightlightMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new HightlightMouseDirective();
    expect(directive).toBeTruthy();
  });
});
